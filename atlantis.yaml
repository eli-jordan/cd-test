version: 3
automerge: true
projects:
- name: stg
  dir: stg
  autoplan:
    when_modified: ["**/*"]
    enabled: true
  apply_requirements: [mergeable]
  workflow: default
workflows:
 default:
   plan:
     steps:
      - run: "/Users/elias.jordan/Desktop/atlantis-hack plan config --environment-id=tapad-paas-eli-operator-test --config-file=environment.json"
   apply:
     steps:
       - run: "echo custom apply"
