version: 3
automerge: true
projects:
- name: main
  dir: .
  autoplan:
    when_modified: ["**/*"]
    enabled: true
  apply_requirements: [mergeable, approved]
  workflow: default
workflows:
 default:
   plan:
     steps:
      - run: "echo custom plan"
   apply:
     steps:
       - run: "echo custom apply"
