version: 3
automerge: true
projects:
- dir: ./stg
  autoplan:
    when_modified: ["*.json"]
    enabled: true
  apply_requirements: [mergeable]
  workflow: default
- dir: ./prd
  autoplan:
    when_modified: ["*.json"]
    enabled: true
  apply_requirements: [mergeable]
  workflow: default
workflows:
 default:
   plan:
     steps:
      - run: "sh /Users/elias.jordan/Desktop/atlantis-hack/cc10.sh plan config --environment-id=$(cat env-id) --config-file=environment.json"
      - run: "touch $PLANFILE"
   apply:
     steps:
       - run: "/Users/elias.jordan/Desktop/atlantis-hack/cc10.sh deploy config --environment-id=(cat env-id) --config-file=environment.json"
